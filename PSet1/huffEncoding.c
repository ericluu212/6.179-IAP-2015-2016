//Huff Encoding

int main() {
	int count;
	scanf("%d", &count);
	int i;
	char code[count][50];
	for (i = 0; i <= count; i = i + 1){
		gets(code[i]);
	}

	/*
	for (i = 0; i <= count; i = i + 1){
		printf("%s",code[i]);
	}
	*/
	char message[100000];
	gets(message);
	puts(message);

	int len;
	for (len = 0; len < 100000; len = len + 1){

		
	}

	return 0;
}